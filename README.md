# vimst

 Clean and efficient configuration for C code development.

This repository holds my personal, easy-to-read Vim configuration (`.vimrc`). It's optimized for a **distraction-free C programming environment**.

---

### ✨ Key Settings

| Feature | Command in `.vimrc` | Purpose |
| :--- | :--- | :--- |
| **Color Scheme** | `colorscheme gruvbox` | High-contrast, dark mode for reduced eye strain. |
| **Indentation** | `set expandtab` `set tabstop=4` | Ensures **4 spaces** are used for indentation (standard for C/C++). |
| **Line Numbers** | `set number` | Essential for quick navigation and debugging. |
| **Plugin Manager** | `vim-plug` | Simple management of necessary plugins (like Gruvbox). |

---
